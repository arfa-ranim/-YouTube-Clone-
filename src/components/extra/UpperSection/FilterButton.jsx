export default function FilterButton({section}) {
    return(
        <div className="rounded-md  bg-slate-400 text-black hover: bg-slate-500" on onClick={} > 
            {section}
        </div>
    )    
}